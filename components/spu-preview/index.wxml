<view class="container">
  <image class="img" src="{{data.img}}"/>
  <view class="contet-container">
    <text class="title">{{data.title}}</text>
    <view class="tags">
      <block wx:for="{{tags}}">
        <l-tag l-class="l-tag" size="super-mini">{{item}}</l-tag>
      </block>
      <view></view>
    </view>
    <l-price>
    <text></text>
    </l-price>
  </view>
</view>